{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "cppdbg",
            "request": "launch",
            "name": "Launch",
            // "preLaunchTask": "Build",
            "program": "${workspaceFolder}/build/handmade.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        },
    ]
}